/// Build metadata shared by the CLI and the menu bar app.
public enum HoneycrispInfo {
    /// The semantic version shown in the panel footer and by the CLI version flag.
    public static let version = "0.1.0"
}
